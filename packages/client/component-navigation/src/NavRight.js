import styled from "styled-components";
import { space, textAlign, width, height } from "styled-system";

export const NavRight = styled.div`
  float: right;
  vertical-align: middle;
  position: relative;
  ${space};
  ${textAlign};
  ${width};
  ${height};
`;
