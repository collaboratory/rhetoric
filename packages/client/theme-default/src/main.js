import colors from "./colors";
import defaults from "./defaults";
import gradients from "./gradients";

export default {
  colors,
  defaults,
  gradients,
  breakpoints: [32, 48, 81, 100],
  space: [0, 6, 12, 18, 24],
  fontSizes: [12, 16, 18, 24, 36, 72]
};
